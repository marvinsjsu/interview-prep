export const sendAnalyticsMessage = (data) => console.log(`Sending analytics message: ${data}`);

export const sendTeamsNotification = (data) => console.log(`Sending message to Teams: ${data}`);

export const sendSlackNotification = (data) => console.log(`Sending message to Slack: ${data}`);